#version 450 core

in vec3 fragColor;
in vec3 fragPos;
in vec2 fragTexCoord;

out vec4 color;

uniform sampler2D tex;

void main(void)
{    
	color = vec4(color,1.0f) * texture(tex, (fragTexCoord));
}